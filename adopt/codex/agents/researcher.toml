# Codex binding for the `researcher` subagent. The brief lives at .agents/briefs/researcher.md
# and is shared with Claude Code, not copied.
name = "researcher"
description = """Scout for the OUTSIDE world. Use to investigate external docs, compare libraries/approaches, check API or version specifics, or surface trade-offs before committing to a design — in an isolated context so only the conclusion returns. Trigger on "what's the best way to / which library / how does <external tool> work / is this still the current API"."""
developer_instructions = """
Your full brief is `.agents/briefs/researcher.md` in this repository. Read it first, ignore its
YAML frontmatter (that is Claude Code tool wiring), and follow the body exactly.

You investigate the outside world for the plan; you never edit code. Synthesize to the
decision and cite sources.
"""

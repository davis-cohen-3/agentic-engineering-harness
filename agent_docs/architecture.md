# Architecture

<FILL: the system shape an agent needs before changing code. A MAP, not a manual —
keep it to what changes how an agent navigates and what it must not break.>

## Components / where things live
- `<component>` — `<responsibility>` — `<path>`

## Boundaries & data flow
<How the pieces talk; what crosses which boundary; the request/data lifecycle.>

## Key invariants
<Rules that must hold — the things a change must not break.>

## Tread carefully
<Areas that are off-limits or need extra care — ties to the risk hotspots in CLAUDE.md.>

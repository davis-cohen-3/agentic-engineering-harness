# Glossary — the domain language

The ubiquitous language of this codebase: what each core term **means here**. Be opinionated
— when several words exist for one concept, pick one and list the rest under `_Avoid_`. Define
what a term **is**, not what it does. Only terms specific to **this** domain — general
programming concepts (timeout, retry, cache) don't belong even if used heavily.

**<Term>**:
<One or two sentences — what it IS in this domain.>
_Avoid_: <synonyms we deliberately don't use>

**<Term>**:
<definition>
_Avoid_: <…>

# agentic-coding-harness

The repo profile lives in `AGENTS.md` and is imported below. It is written once, there, because
Codex reads `AGENTS.md` natively — restating any of it here would create a second copy to keep in
sync, which is the failure this harness exists to prevent.

Put nothing in this file that both providers need. Claude-only guidance goes below the import.

@AGENTS.md
